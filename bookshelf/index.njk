---
layout: base.njk
title: Bookshelf
---
<div class="page-content">

  {# 1. Currently Reading Section #}
  <div style="margin-bottom: 60px; text-align: center;">
    <h2 style="font-family: 'Share Tech Mono', monospace; font-size: 1.2rem; margin-bottom: 15px;">[ currently reading ]</h2>
    {% for book in collections.bookshelf %}
      {% if book.data.status == "currently-reading" %}
        <div class="project-entry" style="display: inline-block; border: 1px dashed #000; padding: 15px 30px;">
          <span style="font-family: 'Share Tech Mono', monospace; font-weight: bold; font-size: 1.2rem;">{{ book.data.title }}</span>
          <p style="font-family: 'Share Tech Mono', monospace; margin: 5px 0 0 0;">{{ book.data.author }}</p>
        </div>
      {% endif %}
    {% endfor %}
  </div>

  {# 2. The Two-Column Grid #}
  <div class="lab-grid">
    
    {# LEFT COLUMN: Reviews (Read) #}
    <div class="bookshelf-column">
      <h2 style="font-family: 'Share Tech Mono', monospace; font-size: 1.1rem; border-bottom: 1px solid #000; padding-bottom: 5px; margin-bottom: 25px;">reviews</h2>
      
      {% for book in collections.bookshelf %}
        {% if book.data.status == "read" %}
          <div class="project-entry" style="margin-bottom: 30px;">
            {# This is the blue link leading to the review #}
            <a href="{{ book.url }}" class="project-title">{{ book.data.title }}</a>
            <p style="font-family: 'Share Tech Mono', monospace; margin-top: -10px; font-size: 0.9rem; color: #000;">
              {{ book.data.author }}
            </p>
          </div>
        {% endif %}
      {% endfor %}
    </div>

    {# RIGHT COLUMN: Reading List (Want to read) #}
    <div class="bookshelf-column">
      <h2 style="font-family: 'Share Tech Mono', monospace; font-size: 1.1rem; border-bottom: 1px solid #000; padding-bottom: 5px; margin-bottom: 25px;">reading list</h2>
      <ul class="lesson-list" style="list-style-type: '>';">
        {% for book in collections.bookshelf %}
          {% if book.data.status == "want-to-read" %}
            <li style="margin-bottom: 15px; font-family: 'Share Tech Mono', monospace;">
              <span style="font-weight: bold; display: block;">{{ book.data.title }}</span>
              <span style="font-size: 0.85rem;">{{ book.data.author }}</span>
            </li>
          {% endif %}
        {% endfor %}
      </ul>
    </div>

  </div>
</div>